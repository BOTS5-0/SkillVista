services:
  - type: web
    name: skillvista
    runtime: node
    buildCommand: npm ci --legacy-peer-deps
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
